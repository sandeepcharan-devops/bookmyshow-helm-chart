apiVersion: v1
kind: Service
metadata:
  name: {{ include "BookMyShow-chart.fullname" . }}
  labels:
    {{- include "BookMyShow-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "BookMyShow-chart.selectorLabels" . | nindent 4 }}
